<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
</head>
<body style="background-color: #1e1e1e; color: #f5f5f5; font-family: Arial, sans-serif;">
    <a href="/journal/<%= userId %>" style="color: #007acc; text-decoration: none; font-size:large;"><= Go Back</a>
    <header style="text-align: center; margin-top: 20px;">
        <h1>Edit Your Note</h1>
    </header>

    <!-- Edit Note Form -->
    <main style="display: flex; flex-direction: column; align-items: center; margin-top: 30px;">
        <section style="width: 100%; max-width: 600px; padding: 20px; background-color: #2d2d2d; border-radius: 8px; margin-bottom: 30px;">
            <form action="/journal/<%= userId %>/edit/<%= note._id %>" method="post">
                <label for="date" style="display: block; margin-bottom: 10px;">Date:</label>
                <input type="date" id="date" name="date" value="<%= note.date.toISOString().split('T')[0] %>" required
                       style="width: 100%; padding: 10px; background-color: #3c3c3c; color: #f5f5f5; border: none; border-radius: 4px; margin-bottom: 15px;">

                <label for="content" style="display: block; margin-bottom: 10px;">Journal Entry:</label>
                <textarea id="content" name="content" rows="6" required
                          style="width: 100%; padding: 10px; background-color: #3c3c3c; color: #f5f5f5; border: none; border-radius: 4px; resize: none; margin-bottom: 15px;"><%= note.content %></textarea>

                <button type="submit" style="padding: 10px 20px; background-color: #007acc; color: #f5f5f5; border: none; border-radius: 4px; cursor: pointer;">Update Note</button>
            </form>
        </section>
    </main>
</body>
</html>
